-- this should remain in the output
module Foo.Bar where

f ::
  Int ->
  Int
f x =

-- @format --arrow-first
-- this should remain in the output
module Foo.Bar where

f
  :: Int
  -> Int
f x = x

-- @format --arrow-last
-- this should remain in the output
module Foo.Bar where

f ::
  Int ->
  Int
f x = x
